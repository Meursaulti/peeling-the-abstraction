
# 笔记

> 持续整理中

---

<details>
<summary><strong>📚 目录（点击展开）</strong></summary>

## C语言编译过程
- [1. 预处理（Preprocessing）](#1-预处理preprocessing)
- [2. 编译（Compilation）](#2-编译compilation)
- [3. 汇编（Assembly）](#3-汇编assembly)
- [4. 链接（Linking）](#4-链接linking)

## 关键路径（Critical Path）完整流程
- [1. 同步路径概览](#1-同步路径概览)
- [2. 时钟上升沿的宏观动作](#2-时钟上升沿的宏观动作)
- [3. 详细流程演进](#3-详细流程演进)
- [4. 关键路径延迟公式](#4-关键路径延迟公式)

## 主锁存器与从锁存器
- [1. 为什么需要建立期（t_setup）](#1-为什么需要建立期t_setup)
- [2. 为什么需要保持期（t_hold）](#2-为什么需要保持期t_hold)
- [3. 数据交接与输出](#3-数据交接与输出)
- [4. 周期循环](#4-周期循环)

</details>

---

# C语言编译过程

写一个 `simple.c`，然后执行：

```bash
gcc simple.c -o simple
```

整个过程大致可以拆成下面几步：

---

## 1. 预处理（Preprocessing）

先做一轮“纯文本替换”：

- `#include` → 头文件直接展开
- `#define` → 宏替换
- `#ifdef` → 条件编译
- 注释会被删掉

为了避免头文件被重复 include，需要用 Header Guard：

```c
#ifndef COMMON_H_
#define COMMON_H_

// 声明

#endif
```

这里要注意：

> 这一阶段完全不懂 C 语义，就是在做字符串替换

输出是 `.i` 文件。

---

## 2. 编译（Compilation）

这一阶段才真正“看懂代码”：

- 语法 / 语义检查
- 生成中间表示（IR）
- 做一些优化
- 最后变成汇编（`.s`）

一个关键点：

> 函数可以没有实现，只要有声明就能过编译

比如：

```asm
call add
```

这里还是符号，不是地址。

---

## 3. 汇编（Assembly）

把 `.s` 转成机器码，生成 `.o`

同时做三件事：

- 指令转二进制
- 建 `symbol table`
- 建 `relocation table`

`relocation table`：

> 对于全局变量的读取 / 存储，以及外部函数调用进行标记，留到链接阶段补全。

---

## 4. 链接（Linking）

链接器生成可执行文件：

- 把多个 `.o` 拼起来
- 找函数实现
- 把之前没填的地址补上

分两种：

### 静态链接

- 从库直接拷需要的目标文件一起链接
- 文件变大，但运行独立

### 动态链接（默认）

- 只保留库的标识
- 运行时再加载动态库
- 节省磁盘空间
- 库更新通常不需要重新编译程序

---

# 第一部分：关键路径（Critical Path）完整流程

同步电路的运行依赖于时钟周期的节奏，数据在寄存器之间传递。

---

## 1. 同步路径概览

```text
        ┌──────────┐
CLK ───▶│ Register A│
        └────┬─────┘
             │
             ▼
      ┌─────────────┐
      │Combinational│
      │   Logic     │
      └────┬────────┘
           │
           ▼
        ┌──────────┐
CLK ───▶│ Register B│
        └──────────┘
```

---

## 2. 时钟上升沿的宏观动作

在每一个时钟上升沿（Rising Edge）触发的瞬间：

- **寄存器 A**：采样并接收当前输入端的新数据 `N`

- **寄存器 B**：采样并接收来自组合逻辑的输出  
  ——这实际上是寄存器 A 在上一个周期接收的数据 `N-1` 经过组合逻辑处理后的结果。

> **核心要求**：寄存器输入必须满足建立时间（Setup Time）与保持时间（Hold Time）约束，否则触发器内部可能进入亚稳态（Metastability），导致输出延迟或逻辑不确定。

---

## 3. 详细流程演进

以下以寄存器 A 的输出为起点进行分析。

### 1. 时钟到 Q 延迟（t_pcq）

时钟上升沿到来后：
- 主锁存器关闭，内部捕获的数据已经稳定（前提是满足 t_setup / t_hold）
- 从锁存器随即打开，主锁存器的状态开始驱动从锁存器内部节点
- 若数据值与从锁存器原先保持的值不同，从锁存器发生翻转
- 翻转后的状态经过一段路径最终稳定的呈现在 Q 端

这一系列内部传播过程所需要的时间称为 **t_pcq（Clock-to-Q delay）**

---

### 2. 组合逻辑处理（t_comb）

信号离开寄存器 A 后，进入组合逻辑网络。

组合逻辑的延迟由：

- 逻辑门延迟
- 导线传播延迟
- 扇出负载
- 晶体管驱动能力

等共同决定。

信号经过逻辑运算后，到达寄存器 B 输入端所需的时间称为：

```text
t_comb
```

这通常是整条同步路径中最大的延迟来源。

---

### 3. 门前数据建立（t_setup）

处理后的信号到达寄存器 B 输入端后，必须在下一个采样边沿到来之前提前稳定。

这一段必须提前稳定的时间称为：

```text
t_setup（建立时间）
```

建立时间存在的根本原因是：

```text
触发器内部需要足够时间，
使内部状态已经明显偏向某个稳定逻辑态
```

这样当采样边沿到来、内部正反馈结构开始主导状态时，系统才能可靠地收敛到确定逻辑值。

如果输入变化过晚，则在采样瞬间，内部节点可能仍接近中间平衡状态，从而导致亚稳态（Metastability）。

---

## 4. 关键路径延迟公式

为了保证系统正常工作，一个时钟周期 `T` 必须能够覆盖整条数据路径的传播时间：

```text
T ≥ t_pcq + t_comb + t_setup
```

如果时钟周期小于该延迟，则在下一个时钟边沿到来时：

- 新数据可能尚未完成组合逻辑传播
- 或寄存器内部尚未建立稳定状态
- 导致建立时间违例（Setup Violation）

此时触发器可能输出错误结果，甚至进入亚稳态。

---

# 主锁存器与从锁存器

为什么寄存器需要建立时间（Setup Time）与保持时间（Hold Time）？

这需要深入到主从 D 触发器（Master-Slave D Flip-Flop）的内部。

---

## 1. 为什么需要建立期（t_setup）？

寄存器的核心是由主锁存器（Master Latch）和从锁存器（Slave Latch）串联组成的。

### 主锁存器透明阶段

当主锁存器处于透明状态时（具体对应哪个时钟电平取决于实现方式），外部输入可以传播到主锁存器内部节点。

### 建立倾向（t_setup 窗口）

在采样边沿到来之前的一小段关键时间内，外部输入必须保持稳定。

这并不只是“给电容充电”，更重要的是：

```text
让锁存器内部节点已经明显偏向某个稳定状态
```

例如内部节点已经形成：

```text
A≈0V
B≈1V
```

或：

```text
A≈1V
B≈0V
```

这样当内部正反馈闭合时，系统才能快速、可靠地收敛到确定逻辑状态。

如果输入在采样边沿前变化过晚，内部节点可能仍接近中间状态（如两侧都接近 `0.5V`），从而导致亚稳态（Metastability）。

---

## 2. 为什么需要保持期（t_hold）？

### 锁存切换瞬间（采样边沿来到）

当采样边沿来到时，主锁存器逐渐关闭，同时内部反馈电路（由交叉耦合反相器组成）开始主导内部状态。

### 再生放大

如果内部节点已经具有明确倾向，正反馈会迅速完成再生过程，将状态收敛到稳定逻辑电平。

例如：

```text
0.1V → 0V
0.9V → VDD
```

### 防止污染（t_hold）

由于真实电路中的传输门与时钟信号都存在有限延迟，锁存器不会在采样边沿瞬间完全隔离输入。

因此，在采样边沿之后的一小段时间内：

```text
输入仍必须保持稳定
```

否则新的输入变化可能继续传播进主锁存器，扰动尚未完全稳定的内部状态，从而导致保持时间违例（Hold Violation）。

---

## 3. 数据交接与输出

与此同时，主锁存器与从锁存器的透明状态发生切换：

1. **从锁存器动作**：从锁存器进入透明状态，允许主锁存器中的状态向输出方向传播。

2. **物理传递**：如果主锁存器已经稳定到确定逻辑状态，其输出会强力驱动从锁存器内部节点，使从锁存器迅速收敛到相同逻辑值。

3. **Q 端稳定**：经过有限传播延迟后，输出端 `Q` 呈现出合法、稳定的新逻辑电平。

---

## 4. 周期循环

当时钟进入下一相位后：

- 主锁存器重新进入透明状态
- 从锁存器关闭并保持当前数据
- 电路准备接收下一轮输入数据

注:可能存在谬误部分仅供参考
